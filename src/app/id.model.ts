export class Id {
  idEvent: number;
  idTicketsCategory: number;

  constructor(idEvent: number, idTicketsCategory: number){
    this.idEvent = idEvent;
    this.idTicketsCategory = idTicketsCategory;
  }
}
